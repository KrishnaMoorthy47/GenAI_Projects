.env
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# The "default" collection is the live portfolio demo's content (Krishna's
# resume + project details) — it ships baked into the Docker image (see
# docker/Dockerfile), so unlike other local/test collections it's committed
# on purpose rather than gitignored.
*.db
*.sqlite
